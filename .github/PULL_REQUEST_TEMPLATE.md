## What was broken or missing

<!-- What couldn't the repository do before this change? -->

## What changed

<!-- The change itself, briefly. The commit message should already say
     this in more detail — see CONTRIBUTING.md. -->

## Testing

- [ ] `npm test` passes (80+ tests)
- [ ] If this fixes a bug: added a test that fails without the fix
- [ ] If this touches `src/naming.mjs` or `src/decisions.mjs`: still
      imports nothing from `node:fs` or `node:child_process`
- [ ] If this adds a dependency: it doesn't — see CONTRIBUTING.md
